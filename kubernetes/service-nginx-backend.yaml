apiVersion: v1
kind: Service
metadata:
  name: servico-ioption-nginx
  labels:
    tier: backend
    app: iqoption
spec:
  type: LoadBalancer
  selector:
    app: iqoption
    tier: nginx
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081

  # externalIPs:
  #   - 192.168.99.100
